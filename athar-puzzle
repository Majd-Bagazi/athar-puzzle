<!DOCTYPE html>
<html lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>أثرك المُلهم</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Cairo:wght@700;900&display=swap'); /* خط عربي قوي */

        body {
            font-family: 'Cairo', sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
            background-color: #f4f4f4;
            padding: 20px;
            text-align: center;
        }
        #result-container {
            position: relative;
            width: 350px; /* يمكن تعديله ليناسب مقاس قالب Canva */
            height: 500px; /* يمكن تعديله ليناسب مقاس قالب Canva */
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            background-color: white;
            border-radius: 15px;
            overflow: hidden;
        }
        #background-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            position: absolute;
            top: 0;
            left: 0;
        }
        #text-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 30px;
            box-sizing: border-box;
            z-index: 10;
            color: #000; /* لون الخط فوق الخلفية */
        }
        #inspirational-quote {
            font-size: 24px;
            font-weight: 900;
            margin-bottom: 20px;
            line-height: 1.5;
        }
        #user-name {
            font-size: 18px;
            font-weight: 700;
            background-color: rgba(255, 255, 255, 0.7); /* خلفية خفيفة للاسم */
            padding: 5px 10px;
            border-radius: 5px;
        }
        #save-button {
            margin-top: 30px;
            padding: 15px 30px;
            font-size: 20px;
            font-weight: 700;
            cursor: pointer;
            background-color: #007bff; /* لون الزر */
            color: white;
            border: none;
            border-radius: 50px;
            box-shadow: 0 4px 10px rgba(0, 123, 255, 0.4);
        }
    </style>
</head>
<body>

    <div id="result-container">
        <img id="background-image" src="https://via.placeholder.com/350x500?text=Your+Canva+Template" alt="خلفية الجملة التحفيزية">
        
        <div id="text-overlay">
            <div id="inspirational-quote"></div>
            <div id="user-name"></div>
        </div>
    </div>

    <button id="save-button" onclick="saveImage()">احفظ أثرك 📸</button>

    <script>
        // دالة لتحليل رابط URL والحصول على الاسم والجملة
        function getQueryParams() {
            const params = {};
            window.location.search.substring(1).split("&").forEach(pair => {
                const [key, value] = pair.split("=");
                params[key] = value ? decodeURIComponent(value.replace(/\+/g, " ")) : "";
            });
            return params;
        }

        // وضع البيانات في الصفحة
        const params = getQueryParams();
        const userName = params.name || "الزائر الكريم";
        const quote = params.quote || "أثرك يملأ الدنيا نورًا.";

        document.getElementById('user-name').textContent = `— ${userName}`;
        document.getElementById('inspirational-quote').textContent = quote;

        // دالة حفظ الصورة
        function saveImage() {
            // إخفاء الزر قبل التقاط الصورة لعدم ظهوره فيها
            document.getElementById('save-button').style.display = 'none';

            html2canvas(document.getElementById('result-container'), {
                allowTaint: true,
                useCORS: true,
                scale: 2 
            }).then(function(canvas) {
                // إظهار الزر مرة أخرى
                document.getElementById('save-button').style.display = 'block';

                const image = canvas.toDataURL("image/png");
                const link = document.createElement('a');
                link.href = image;
                link.download = `أثرك_يا_${userName}.png`;
                document.body.appendChild(link);
                link.click();
                document.body.removeChild(link);
            });
        }
    </script>

</body>
</html>
